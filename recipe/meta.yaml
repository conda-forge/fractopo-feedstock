{% set name = "fractopo" %}
{% set version = "0.2.5" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/fractopo-{{ version }}.tar.gz
  sha256: 927152fadea96c9072576a03236948b596a7fdca3b56cddc5a65b96d1fd145ba

build:
  number: 0
  noarch: python
  entry_points:
    - fractopo = fractopo.cli:app
    - tracevalidate = fractopo.cli:tracevalidate_click
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.7.1,<4.0
    - poetry >=1.1,<2
  run:
    - click
    - descartes
    - geopandas >=0.10.2,<0.11.0
    - matplotlib-base
    - numpy
    - pandas >=1.3,<1.4
    - powerlaw
    - pygeos >=0.12.0,<0.13.0
    - python >=3.6,<4.0
    - python-ternary
    - rich >=10.7.0,<11.0.0
    - scikit-learn
    - scipy
    - seaborn
    - shapely >=1.8.0,<1.9.0
    - typer >=0.3.2,<0.4.0

test:
  imports:
    - fractopo
    - fractopo.analysis
  commands:
    - pip check
    - fractopo --help
    - tracevalidate --help
  requires:
    - pip

about:
  home: https://github.com/nialov/fractopo
  summary: Fracture Network Analysis
  license: MIT
  license_family: MIT
  license_file: LICENSE.md
  doc_url: https://fractopo.readthedocs.io/en/latest/
  dev_url: https://github.com/nialov/fractopo

extra:
  recipe-maintainers:
    - nialov
